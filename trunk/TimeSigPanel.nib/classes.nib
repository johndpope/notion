{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {
                keySigChanged = id; 
                keySigClose = id; 
                timeSigBottomChanged = id; 
                timeSigClose = id; 
                timeSigTopChanged = id; 
            }; 
            CLASS = Measure; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                keySigLetter = NSPopUpButton; 
                keySigMajMin = NSPopUpButton; 
                keySigPanel = NSView; 
                timeSigBottom = NSPopUpButton; 
                timeSigPanel = NSView; 
                timeSigTopStep = NSStepper; 
                timeSigTopText = NSTextField; 
            }; 
            SUPERCLASS = NSObject; 
        }
    ); 
    IBVersion = 1; 
}