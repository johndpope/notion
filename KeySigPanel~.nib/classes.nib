{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {keySigChanged = id; keySigClose = id; }; 
            CLASS = Measure; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                keySigLetter = NSPopUpButton; 
                keySigMajMin = NSPopUpButton; 
                keySigPanel = NSView; 
            }; 
            SUPERCLASS = NSObject; 
        }
    ); 
    IBVersion = 1; 
}